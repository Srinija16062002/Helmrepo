apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-deployment
  labels:
    app: swiggy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: swiggy
  template:
   metadata: 
     name: mytemp
     labels:
       app: swiggy
   spec:
    containers:
     - name: cont1
       image: nginx
       ports:
        - containerPort: 80
